{
  "dependencies": {
    "axios": "^1.10.0",
    "mongodb": "^6.17.0",
    "react-router-dom": "^7.6.2"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  },
  "scripts": {
    "start:mock": "json-server --watch db.json --port 3000"
  }
}
